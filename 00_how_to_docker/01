docker-machine create --driver virtualbox Char 

//instead you can use this one : 

docker-machine create --driver virtualbox --virtualbox-hostonly-cidr "192.168.56.1/24" Char

//docker-machine is old so I used the multipass

multipass launch --name Char
multipass shell Char

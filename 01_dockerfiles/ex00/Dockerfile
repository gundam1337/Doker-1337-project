# Use Alpine as the base image
FROM alpine:latest

# Install your chosen editor (vim in this example)
RUN apk add --no-cache vim

# Set the editor as the default command when the container runs
CMD ["vim"]
